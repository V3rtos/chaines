stages:
  - build
  - test

cache:
  key: new_cache
  paths:
    - .m2/repository/

build:
  image: maven:3-jdk-8
  stage: build
  variables:
    MAVEN_OPTS: "-Dmaven.repo.local=/root/.m2/repository"
  tags: [moonways-runner, Joule]
  script: /bin/bash bridgenet -fb
